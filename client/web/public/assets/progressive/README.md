# Progressive Asset Runtime — Web

Os packs aprovados são instalados localmente neste diretório por `scripts/install_progressive_pack.py`.

Os binários permanecem fora do Git. Cada pack utiliza um namespace próprio e não substitui o bootstrap anterior antes da promoção completa.

Tehkné Solutions
